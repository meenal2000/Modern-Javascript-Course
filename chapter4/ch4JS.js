// Adding and removing classes
// const content = document.querySelector('p'); 
// console.log(content.classList); // shows all the classes associated with the content
// content.classList.add('success');
// content.classList.remove('success');
// console.log(content.classList);

// query selector returns a node-list and we can easily traverse thorugh
// using a forEach
 
// even though the text is hidden , textContent will show and innerText will hide
We can also do toggle for adding,removing classes.